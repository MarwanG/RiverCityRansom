service: Terrain
use: Object, Bloc
types : int,boolean 

observators:
	- const width: [Terrain] -> int
	- const height: [Terrain] -> int
	- const depth: [Terrain] -> int
	- bloc_pos [Terrain] * int * int * int -> Bloc

constructors:
	- init: int * int * int -> [Terrain]

operators:
	- setBloc: int * int * int * Bloc -> [Terrain]
		pre: setBloc(x,y,z,b) require x<width AND x>=0 AND y<height AND y>=0 AND z<depth AND z>=0 AND b != NULL
